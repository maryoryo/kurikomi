<div class="container my-5">
  
  <div class="row justify-content-center">
    
    <div class="card border-dark py-4 px-5">
      <h2 class="font-name name">パスワードの再設定</h2>
      
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "admin/admins/shared/error_messages", resource: resource %>
      
        <div class="field mt-3">
          <%= f.label :メールアドレス %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
        </div>
      
        <div class="actions mt-3">
          <%= f.submit "送信する", class:"font-name btn btn-info" %>
        </div>
      <% end %>
      
      <div class="mt-3">
        <%= render "admin/admins/shared/links" %>
      </div>
    </div>
  </div>
</div>