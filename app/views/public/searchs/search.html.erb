<div class="container my-5 col-8">
  <div class="row">
    
    <% if @model == "user" %>
      <%= render 'public/users/index', users: @records %>
    <% elsif @model == "group" %>
      <%= render 'public/groups/index', groups: @records %>
    <% elsif @model == "genre" %>
      <%= render 'public/genres/index', genres: @records %>
    <% else %>
      <h3>見つかりませんでした。検索条件を変更して再度お試しください。</h3>
    <% end %>
  </div>
</div>